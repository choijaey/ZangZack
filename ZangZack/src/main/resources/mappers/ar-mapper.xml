<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.zangzac.yoonahrim.model.dao.secondHandDAO">
	<insert id="insertSeconHand">
		insert into SECONDHAND_PRODUCT
		values (SP_SEQ.nextval, #{spTitle}, #{spPrice}, #{spContent}, #{spAddress}, default, default, 1, #{categoryNo}, default) 
	</insert>

	<update id="updateSeconHand">
		update secondHandProduct
		set SP_NO = SP_SEQ.nextval, 
			SP_TITLE = #{spTitle}, 
			SP_PRICE = #{spPrice}, 
			SP_CONTENT = #{spContent}, 
			SP_ADDRESS = #{spAddress}, 
			CATEGORY_NO = #{categoryNo}
		where id = #{memberId}
	</update>
	
	
	
	
</mapper>